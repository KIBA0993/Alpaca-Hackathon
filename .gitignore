.env
.env.*
!.env.example
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
.streamlit/secrets.toml

# NOT ignored: logs/*.jsonl. The decision journal is the deliverable — it ships
# so anyone can check a decision against the record. It holds no keys, no
# account identifiers and no personal data.

# local working notes + config backups (not part of the public submission)
docs/SUBMISSION.md
config.json.bak*

# LibreOffice lock files (from PDF rendering during QA)
.~lock.*#

# Local configuration variants — not part of this submission. Kept on disk so
# the parameterised deploy script still works locally; simply not published.
config.arm*.json
nas/docker-compose.arm*.yml
tests/test_config_arms.py

# local editor/launch scratch
.claude/
