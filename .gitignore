.env
.env.*
!.env.example
__pycache__/
*.pyc
.pytest_cache/
logs/
*.jsonl
.DS_Store

# local working notes + config backups (not part of the public submission)
docs/SUBMISSION.md
config.json.bak*

# LibreOffice lock files (from PDF rendering during QA)
.~lock.*#

# Sibling experiment arms (B/C/D) — not part of this submission. Kept on disk so
# `ARM=B nas/deploy-to-nas.sh` still works locally; simply not published.
config.arm*.json
nas/docker-compose.arm*.yml
tests/test_config_arms.py
