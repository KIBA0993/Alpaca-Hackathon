# Keep secrets and cruft OUT of the build context (so they can never land in an
# image layer). .env is injected at runtime via compose, never copied.
.env
.env.*
!.env.example
*.bak
config.json.bak*
__pycache__/
**/__pycache__/
.pytest_cache/
logs/
.git/
docs/
